'use strict';

describe('myApp.associate module', function () {

    beforeEach(module('myApp.associate'));

    describe('associate controller', function () {

        it('should ....', inject(function ($controller) {
            var associateCtrl = $controller('AssociateCtrl');
            expect(associateCtrl).toBeDefined();
        }));

    });
});